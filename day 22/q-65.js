"use strict";
// Determine the Remainder: Make a function that gets two numbers and shows the leftover from dividing them using
//  the % sign. For example, remainder(5, 2)
// should give 1.
function remainder(val1, val2) {
    console.log(val1 % val2);
}
remainder(5, 2);
