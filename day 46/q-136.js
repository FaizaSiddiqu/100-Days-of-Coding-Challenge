// Use console.log() to debug and track the value of a variable inside a loop.
// demonstrate using console.log() insikde a loop to track variable outside.
for (var i = 1; i <= 5; i++) {
    console.log("Iteration ".concat(i, ", i value: , i")); //logs the current iteration number and the valu of i
}
/// This loop iterates five times, logging the value of 'i' during each iteration to help with debugging.
